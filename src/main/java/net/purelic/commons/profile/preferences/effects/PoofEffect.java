package net.purelic.commons.profile.preferences.effects;

import org.bukkit.Location;

public class PoofEffect implements CustomEffect {

    @Override
    public void play(Location location) {
        // default effect has no impl since we always send the vanilla death animation
    }

}
